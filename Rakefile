# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/uridium.rb'

Hoe.new('uridium', Uridium::VERSION) do |p|
  # p.rubyforge_name = 'uridiumx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
